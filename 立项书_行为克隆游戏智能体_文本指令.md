# 课题六立项书：行为克隆游戏智能体（文本指令方向）

| 项目 | 内容 |
|---|---|
| 课题名称 | 行为克隆游戏智能体（腾讯IEG课题 · 课题六） |
| 参考项目 | Open Pixel2Play (Open P2P)，https://elefant-ai.github.io/open-p2p/ |
| 论文 | *Scaling Behavior Cloning Improves Causal Reasoning: An Open Model for Real-Time Video Game Playing*（arXiv:2601.04575） |
| 立项扩展方向 | **文本指令（Text Instruction）** |
| 组数上限 | 4 组 |
| 组名/成员 | （待填写） |
| 立项日期 | 2026-08-19 |

---

## 一、课题背景与意义

### 1.1 背景

行为克隆（Behavior Cloning, BC）是模仿学习的一种核心方法：从人类专家的状态—动作轨迹中学习策略，将观测（如游戏画面）映射为动作（如键鼠操作）。近年来，大语言模型（LLM）与多模态架构的进展使"视觉-语言-动作"（Vision-Language-Action）模型成为游戏智能体的重要范式：模型不仅"看"画面，还能理解**文本指令**并据此产生符合意图的键鼠动作，这为"可指挥的游戏 AI"奠定了基础。

Open Pixel2Play（Open P2P）是本课题指定的官方参考实现，具备以下关键特性：

- 以**图像序列 + 可选文本指令**为输入，输出**键盘/鼠标动作**，实现与真实游戏环境的直接交互；
- 开源了 150M / 300M / 600M / 1200M 多档预训练检查点，其中 **150M 模型**为课题指定最小复现单元（官方约 8×H100 训练 70 小时得到）；
- 官方提供完整推理脚本（`elefant/policy_model/inference.py`）与数据下载脚本；
- 模型架构原生支持**文本指令输入**（`--input_text` 启动开关），与课题组选定的扩展方向直接契合；
- 提供 p2p-full-data（8,000+ 小时人工标注游戏视频）与 p2p-toy-examples（小样数据集）两档数据。

### 1.2 选题价值

1. **可复现性**：基于官方开源权重与脚本，MVP 阶段即可在受限算力下获得可量化的行为克隆基线，成果可被他人复现；
2. **探索性**：官方论文中绝大多数实验采用**无文本指令**的默认设置，文本指令模式的可玩性高、研究空间大，符合"立项扩展"的定位；
3. **可评测性**：任务完成率、按键准确率、鼠标相关系数均为可量化指标，便于横向对比与汇报。

---

## 二、研究目标与考核指标

### 2.1 总体目标

围绕官方 150M 预训练模型，完成"环境搭建 → 推理复现 → 评测基线 → 录屏演示 → 归档报告"全流程，并在此基础上完成**文本指令**扩展方向的实验与对比，证明：**文本指令能显著提升模型在特定指令任务上的完成率**。

### 2.2 MVP 考核指标（5 项）

| 编号 | MVP 要求 | 本组交付 | 量化验收标准 |
|---|---|---|---|
| M1 | 跑通官方 150M 预训练模型推理脚本，README 可复现 | 环境配置文档 + 复现 README + 推理运行记录 | 在 Linux/WSL 下按 README 步骤可一键复现推理输出 |
| M2 | 编写评测脚本：测试集 ≤200 帧（与训练/微调数据隔离），报告按键准确率、鼠标位移误差或相关系数 | `evaluate.py` 评测脚本 + 指标表（CSV/Markdown） | 测试集帧数 ≤200；输出按键准确率 + 鼠标相关系数两项指标 |
| M3 | 未微调基线：按键准确率 ≥55%，鼠标相关系数 ≥0.5（Toy 或所选单一游戏测试集） | 基线评测结果表 | 未微调 150M 模型在测试集上：按键准确率 ≥55% 且鼠标相关系数 ≥0.5 |
| M4 | 第 5 天录屏演示同一测试片段上的模型输出与人类标注对比 | 对比录屏（含人类标注轨迹叠加） | 同一测试片段上模型输出与人类标注的可视化对比 |
| M5 | 归档代码、指标表、3000 字以上实验报告 | 代码归档（Git 仓库）+ 指标表 + 实验报告 | 实验报告正文 ≥3000 字 |

### 2.3 立项扩展指标（文本指令方向）

| 指标 | 目标值 | 评测口径 |
|---|---|---|
| 指令片段数量 | **2 个**指令片段（instruction episodes） | 每个片段 = 一段连续游戏视频 + 1 条明确指令（含开始帧与结束帧） |
| 带指令任务完成率 | **≥60%** | 模型输入 = 当前帧 + 指令文本，按自动判定规则判断任务是否完成 |
| 不带指令任务完成率 | **≤35%**（对照组） | 模型输入 = 当前帧，无指令文本，同规则判定 |
| 核心结论 | 带指令 − 不带指令 ≥ +25 个百分点 | 证明文本指令的有效增益 |

> 算力来源：项目自备/申请的 NVIDIA GPU（详见第五节）。模型推理阶段单卡 8–16GB 显存即可运行 150M 模型；微调阶段建议 16GB 以上。

---

## 三、技术路线

### 3.1 总体架构

```
游戏视频帧(观测) ──┐
                   ├──> Open P2P 150M 策略模型 ──> 键盘动作 + 鼠标位移
文本指令(可选) ────┘         │
                              ▼
              评测脚本 evaluate.py
    (按键准确率 / 鼠标相关系数 / 任务完成率)
```

- **输入**：图像（训练分辨率 192×192）+ 可选文本指令；
- **输出**：键位动作（key events）与鼠标位移（mouse displacement）；
- **对照实验**：同一测试片段，分别以「带指令 / 不带指令」两种模式运行，量化指令增益。

### 3.2 阶段划分

| 阶段 | 内容 | 对应 MVP |
|---|---|---|
| S1 环境搭建 | 安装 uv、FFmpeg、Clang、Rust、GPU 驱动；`git clone open-p2p`；`uv sync`；下载 150M 检查点与 p2p-toy-examples 数据 | M1 |
| S2 推理复现 | 先以 `--use_random_weights` 验证链路，再加载官方 150M 检查点完成真实推理；使用 `--input_text` 验证文本指令输入模式 | M1 |
| S3 数据划分 | 从 toy 数据（必要时叠加 full-data 指定游戏）中划分训练/微调集与 **≤200 帧测试集**，保证测试集与训练/微调数据完全隔离 | M2 |
| S4 评测脚本 | 实现 `evaluate.py`：逐帧读取模型输出与人类标注，计算按键准确率、鼠标位移相关系数/误差 | M2 |
| S5 基线评测 | 未微调 150M 模型在测试集上评测，验证 M3 双指标达标 | M3 |
| S6 文本指令扩展 | 设计 2 个指令片段；分别以带/不带指令运行模型，计算任务完成率；与人类标注对比录屏 | 扩展方向 / M4 |
| S7 归档报告 | 整理代码、指标表、录屏，撰写 ≥3000 字实验报告 | M5 |

### 3.3 关键技术点

1. **文本指令模式约束**：官方实现中文本输入模式必须在**启动时**通过 `--input_text` 指定，运行期间无法动态切换，故带指令/不带指令两组实验需分别启动推理进程；
2. **评测帧对齐**：以人类标注的帧索引为准，对模型输出做时间对齐（同一视频帧输入模型，比较同一帧的预测与标注）；
3. **指令片段设计**：选择动作意图明确、可自动判定的指令（如"向左移动""跳跃""转向并射击"），使完成标准可量化，避免人工判定的主观性；
4. **数据隔离**：测试集片段不参与任何训练/微调/提示工程过程，确保指标可信。

---

## 四、数据说明

| 数据集 | 用途 | 规模与格式 |
|---|---|---|
| p2p-toy-examples | 主线数据源：推理验证、测试集选取、指令片段素材 | 小样数据；视频帧 + `annotation.proto` 键鼠动作标注 |
| p2p-full-data | 按需补充：选定单一游戏（如 Roblox Rivals）的更多帧用于微调/指令片段 | 8,000+ 小时人工标注视频，分批次（batch）下载 |

数据格式要点：

- 人类标注为**逐帧键鼠动作**（`annotation.proto`），含键盘事件与鼠标位移，可直接与模型输出逐帧对齐；
- 训练默认分辨率 192×192，无需重采样（如改用其他分辨率，按官方 `rescale` 流程处理）。

数据划分方案（隔离原则）：

- **训练/微调集**：用于微调实验的帧（扩展方向如需，可对指令片段同款游戏数据微调，本组立项目标以预训练模型直接评测为主）；
- **测试集**：从 toy 数据中**固定**选取 ≤200 帧（约 1 个连续片段），与任何训练/微调样本无重叠，冻结不变，保证多次实验可比。

---

## 五、算力与环境需求

| 资源 | 需求 | 说明 |
|---|---|---|
| GPU | 单卡 NVIDIA GPU（显存 ≥8GB 可推理；微调建议 ≥16GB） | 150M 模型推理显存需求低；微调用官方 `train.py` |
| 系统 | Linux / WSL2 + Ubuntu 24.04（Windows 实时推理场景需 WSL + 双卡） | 本项目以离线推理与评测为主，不依赖实时端到端 <50ms 链路 |
| 存储 | ≥100GB（full-data 按需下载） | 可先只用 toy 数据，full-data 按需下载 batch 1–5 |
| 软件 | uv、FFmpeg ≥7、Clang、Rust、HuggingFace CLI | 按官方 README 安装，写进复现 README |
| 网络 | 可访问 HuggingFace（需登录获取 Gemma tokenizer 认证） | 若网络受限，提前缓存检查点 |

算力来源：本组自行申请/借用（如云 GPU 实例、实验室服务器），推理与评测阶段单卡即可满足；微调（如启用）按官方 8×H100 预算的缩减比例折算。

---

## 六、评测方案与指标定义

### 6.1 按键准确率（Key Accuracy）

逐帧比较模型预测的按键集合与人类标注的按键集合（含"无按键"帧）：

```
按键准确率 = 预测按键完全匹配的帧数 / 总评测帧数 × 100%
```

- 单帧匹配：预测按键集合 == 标注按键集合（包括空集）；
- 总评测帧数 ≤200。

### 6.2 鼠标位移误差与相关系数

- **位移误差**：鼠标位移的逐帧绝对误差均值（MAE）或欧氏距离误差；
- **相关系数**：模型预测位移序列 X 与人类标注位移序列 Y 的 Pearson 相关系数：

```
r = cov(X, Y) / (σ_X · σ_Y)
```

- 达标口径（MVP）：**r ≥ 0.5**；
- 报告时同时给出 r 值与位移 MAE。

### 6.3 任务完成率（文本指令扩展）

对每个指令片段定义**可自动判定的完成标准**，例如：

| 指令片段示例 | 完成标准（自动判定） |
|---|---|
| 片段 A："持续向左移动" | 片段结束时模型累计左移位移超过阈值，且按键中持续包含左移键 |
| 片段 B："跳跃后向右转向" | 在片段内按顺序检测到跳跃键与右转向动作序列 |

```
任务完成率 = 判定为完成的片段数 / 指令片段总数 × 100%
```

- **带指令组**：`--input_text` 启动，输入 = 帧 + 指令文本；
- **不带指令组**：默认模式启动，输入 = 帧；
- 目标：带指令 ≥60%，不带指令 ≤35%，增益 ≥+25pp。

### 6.4 人类标注对比录屏（M4）

同一测试片段上，将以下内容叠加到视频帧后录屏：

1. 人类标注的鼠标轨迹与按键事件；
2. 模型输出的鼠标轨迹与按键事件（逐帧同步回放）。

以可视化证明模型输出与人类行为的对齐程度，作为第 5 天演示素材。

---

## 七、时间计划（5 天）

| 时间 | 任务 | 里程碑/产出 |
|---|---|---|
| Day 1 | 环境搭建：uv/FFmpeg/Clang/Rust、clone 仓库、`uv sync`；下载 150M 检查点与 toy 数据 | 环境可运行 `inference.py` |
| Day 2 | 推理复现：随机权重验证 → 加载真实检查点推理；验证 `--input_text` 模式；固定测试集片段（≤200 帧） | M1 达成；测试集冻结 |
| Day 3 | 编写 `evaluate.py`；未微调基线评测，产出指标表 | M2、M3 达成（按键准确率 ≥55%，鼠标 r ≥0.5） |
| Day 4 | 文本指令扩展：设计 2 个指令片段、实现任务完成率判定；带/不带指令对照实验；制作对比录屏 | 扩展指标达标；M4 素材就绪 |
| Day 5 | 归档代码与指标表；撰写 ≥3000 字实验报告；录屏演示准备与答辩 | M5 达成，全部交付物就绪 |

> 若 Day 3 基线指标未达标：优先排查评测对齐逻辑、帧采样方式与指令输入格式；可换用更利于动作对齐的单一游戏（如 Roblox Rivals）测试集。

---

## 八、风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| HuggingFace 登录/Gemma tokenizer 认证失败 | 无法加载模型 | 提前 `huggingface-cli login`；检查点与 tokenizer 本地缓存；准备代理/镜像 |
| 文本指令模式编译约束（仅启动时指定） | 实验设计受限 | 带/不带指令分别启动两个推理进程；提前确定指令文本集合 |
| full-data 下载量大、网络慢 | 进度延误 | MVP 阶段只用 toy 数据；full-data 按需下载 batch 1–5 |
| 显存/算力不足 | 微调无法进行 | 扩展方向以"预训练模型 + 指令"为主，不依赖微调；微调仅作加分项 |
| 指标不达标（按键/鼠标） | 无法通过验收 | 检查帧对齐与评测口径；选择标注更稳定的单一游戏；必要时补充 full-data 同游戏帧 |
| Windows 实时推理复杂 | 影响演示形式 | 以离线评测 + 录屏对比满足 M4，实时游戏推理作为可选项 |

---

## 九、预期成果与交付物

1. **代码归档**：Git 仓库，含复现 README、`evaluate.py` 评测脚本、指令片段判定脚本、实验配置；
2. **指标表**：未微调基线（按键准确率 / 鼠标相关系数 / 位移误差）+ 带指令/不带指令任务完成率对比表；
3. **录屏演示**：同一测试片段上模型输出 vs 人类标注对比（MVP4）；
4. **实验报告**：≥3000 字，含实验设计、方法、结果、对比分析、结论与展望；
5. **扩展成果**：文本指令增益结论（带指令 ≥60% vs 不带指令 ≤35%）。

---

## 十、参考文献

1. Yuguang Yue, Irakli Salia, Samuel Hunt, Chris Green, Wenzhe Shi, Jonathan J. Hunt. *Scaling Behavior Cloning Improves Causal Reasoning: An Open Model for Real-Time Video Game Playing*. arXiv:2601.04575, 2026.
2. Open Pixel2Play 官方文档与源码：https://elefant-ai.github.io/open-p2p/ ；https://github.com/elefant-ai/open-p2p
3. Pomerleau, D.A. *ALVINN: An Autonomous Land Vehicle in a Neural Network*. NeurIPS, 1988.（行为克隆经典工作）
4. Florence, P., et al. *Implicit Behavioral Cloning*. CoRL, 2021.（行为克隆方法论）

---

*本立项书中的指标、时间与算力来源均已写入，后续实验如遇实际情况变化，将在实验报告中如实记录并修订。*
