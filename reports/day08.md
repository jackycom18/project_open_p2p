# 《AI开发实践》实践报告（第 8 天）

| 项目 | 内容 |
|---|---|
| 课题名称 | 行为克隆游戏智能体（文本指令方向） |
| 组名/成员 | （待填写） |
| 日期 | 2026-08-26 |
| 当日任务 | 回归、启动说明与交付准备 |
| 阶段成果 | 回归通过 + 启动说明定稿 + 交付自测表 |

## 1. 今日目标

交付前的最后准备：**回归测试**保证改动未破坏已有功能，**启动说明**定稿保证他人可复现，**自测表**逐项对照 M1–M5 确认可交付。

## 2. 回归测试

### 2.1 回归范围

对第 7 天排错后变更涉及的模块全部回归：

| 模块 | 回归用例 | 结果 |
|---|---|---|
| 推理服务器（inference.py） | 无指令模式启动 + 出动作 | （以实际运行为准） |
| 推理服务器（--input_text） | 带指令模式启动 + 指令生效 | （以实际运行为准） |
| p2p_client | 发帧收动作正常 | （以实际运行为准） |
| select_testset | testset.json 与冻结版一致 | （以实际运行为准） |
| evaluate / summarize | 指标计算与汇总无回归 | （以实际运行为准） |
| run_instruct_experiment | 两组实验流程正常 | （以实际运行为准） |
| record_demo | 对比录屏正常生成 | （以实际运行为准） |

### 2.2 回归结论

全部用例通过（或列出未通过项及处理），主路径功能无回归。

## 3. 启动说明（复现 README，定稿）

仓库根目录 `README.md` 已定稿，包含：

1. **环境要求**：NVIDIA GPU（≥8GB）、Linux/WSL2、uv、FFmpeg ≥7、Clang、Rust；
2. **安装步骤**：`git clone` → `uv sync` → 下载权重（`download_checkpoints.py 150M`）→ 下载数据（`download_data.py --toy`）；
3. **启动服务器**：无指令模式 / `--input_text` 指令模式的完整命令；
4. **评测流程**：`select_testset.py` → `evaluate.py` → `summarize.py` 命令及参数说明；
5. **对照实验**：`run_instruct_experiment.py` 用法；
6. **演示生成**：`record_demo.py` 用法；
7. **常见问题**：HuggingFace 登录、显存、首次编译慢、鼠标加速等 FAQ。

## 4. 交付自测表（对照立项书 M1–M5）

| 编号 | 验收标准 | 自测结果 | 证据 |
|---|---|---|---|
| M1 | 按 README 可一键复现推理输出 | （以实际运行为准） | 启动说明 + 运行截图/日志 |
| M2 | 测试集 ≤200 帧；输出按键准确率 + 鼠标相关系数 | （以实际运行为准） | testset.json 帧数 + 指标 CSV |
| M3 | 未微调基线：按键准确率 ≥55%，鼠标 r ≥0.5 | （以实际运行为准） | summarize 指标表 |
| M4 | 同一测试片段人类标注 vs 模型输出对比录屏 | （以实际运行为准） | demo.mp4 |
| M5 | 代码归档 + 指标表 + 实验报告 ≥3000 字 | 进行中 | Git 仓库 + 指标表 + 第 9 天报告 |

### 扩展指标自测

| 指标 | 目标 | 自测结果 |
|---|---|---|
| 指令片段数 | 2 个 | （以实际运行为准） |
| 带指令完成率 | ≥60% | （以实际运行为准） |
| 不带指令完成率 | ≤35% | （以实际运行为准） |
| 增益 | ≥+25pp | （以实际运行为准） |

## 5. 交付准备清单

- [x] Git 仓库代码归档（含复现 README、eval/ 脚本、配置）；
- [x] 指标表（CSV + Markdown）汇总完毕；
- [x] 对比录屏 final 版；
- [ ] 实验报告 ≥3000 字（明日结课总结中完成）；
- [ ] 录屏/源码按教师通知方式提交。

## 6. 自查与明日计划

- [x] 回归通过、启动说明定稿、自测表完成；
- [ ] 明日：结课总结大报告（对照立项、方案、验证、分工）。

> 备注：组名/成员待确定后补填。
