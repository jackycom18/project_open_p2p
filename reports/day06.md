# 《AI开发实践》实践报告（第 6 天）

| 项目 | 内容 |
|---|---|
| 课题名称 | 行为克隆游戏智能体（文本指令方向） |
| 组名/成员 | （待填写） |
| 日期 | 2026-08-24 |
| 当日任务 | 推进两块可验证的工作 |
| 阶段成果 | 两块工作完成自测并产出结果 |

## 1. 今日目标

集中推进**两块可独立验证**的工作，各做一轮自测：

- **工作块 A：未微调基线评测**（对应 M3：按键准确率 ≥55%，鼠标相关系数 ≥0.5）；
- **工作块 B：文本指令对照实验**（对应扩展指标：2 个指令片段，带指令完成率 ≥60%，不带指令 ≤35%，增益 ≥+25pp）。

## 2. 工作块 A：未微调基线评测

### 2.1 做法

1. 用 `select_testset.py` 从 toy 数据固定挑选 ≤200 帧测试集并冻结（`testset.json`）；
2. 以**不带指令**模式启动推理服务器（真实 150M 权重）；
3. 用 `evaluate.py` 逐帧评测，输出按键准确率与鼠标相关系数。

### 2.2 自测记录

| 评测片段 | 帧数 | 按键准确率 | 鼠标相关系数 r | 位移 MAE |
|---|---|---|---|---|
| testset.json（≤200 帧） | （以实际运行为准） | （以实际运行为准） | （以实际运行为准） | （以实际运行为准） |

> 说明：表中数值为待填入项，正式指标以冻结测试集 + 真实权重运行为准，杜绝假数据入表（第 3 天约定）。

### 2.3 自测结论

- 评测脚本可稳定产出两项指标（流程自测通过）；
- 达标与否待数值回填后判定；若未达标，执行预案：检查帧对齐/采样逻辑 → 换标注更稳定的单一游戏测试集（如 Roblox Rivals）→ 必要时补充 full-data 同游戏帧。

## 3. 工作块 B：文本指令对照实验

### 3.1 指令片段设计（2 个）

按"动作意图明确、可自动判定"原则设计：

| 片段 | 指令文本 | 完成标准（自动判定） |
|---|---|---|
| 片段 A | "持续向左移动" | 片段结束时累计左移位移超过阈值，且按键中持续包含左移键 |
| 片段 B | "跳跃后向右转向" | 片段内按顺序检测到跳跃键与右转向动作序列 |

每个片段 = 一段连续游戏视频 + 1 条明确指令（含开始帧与结束帧），来自 toy 数据裁剪。

### 3.2 实验设计

- **对照组**：默认模式启动服务器（无指令），输入 = 帧；
- **实验组**：`--input_text` 启动服务器，评测前注入指令文本，输入 = 帧 + 指令；
- 用 `run_instruct_experiment.py` 自动完成两组实验，输出对比 CSV + Markdown 表；
- 两组成对比较，量化指令增益。

### 3.3 自测记录

| 片段 | 带指令完成率 | 不带指令完成率 | 增益 |
|---|---|---|---|
| 片段 A | （以实际运行为准） | （以实际运行为准） | （以实际运行为准） |
| 片段 B | （以实际运行为准） | （以实际运行为准） | （以实际运行为准） |

### 3.4 自测结论

- 对照实验脚本已跑通（两组分别启动、注入指令、判定完成）✅；
- 目标口径：带指令 ≥60%、不带指令 ≤35%、增益 ≥+25pp，数值待回填后判定。

## 4. 遇到的问题与解决

| 问题 | 处理 |
|---|---|
| 指令模式启动时固定，运行中不可切换 | 两组实验分别启动两个推理进程，进程间不共享状态（第 3 天约定） |
| 指令判定存在主观性风险 | 完成标准全部写成自动判定规则（位移累计/按键序列），不依赖人工目测 |

## 5. 自查与明日计划

- [x] 工作块 A：基线评测流程自测通过，指标可稳定产出；
- [x] 工作块 B：对照实验脚本自测通过，两组实验可自动完成；
- [ ] 明日：贯通验证与排错——端到端全链路通检，回填正式指标，处理遗留问题。

> 备注：组名/成员待确定后补填。
